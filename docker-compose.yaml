services:
  smartcam:
    build: .
    container_name: smartcam
    restart: unless-stopped
    network_mode: host
    environment:
      - NODE_ENV=production
      - APP_URL=${APP_URL:-}
    env_file:
      - .env